<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="io.futurestud.tutorials.picasso"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.activities.UsageExampleListActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.UsageExampleDetailActivity"
            android:label="@string/title_usageexample_detail"
            android:parentActivityName=".ui.activities.UsageExampleListActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activities.UsageExampleListActivity" />
        </activity>
    </application>

</manifest>
